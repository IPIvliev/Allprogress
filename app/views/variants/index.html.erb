<!-- WRAPPER -->
        <div id="wrapper">

            <!-- PAGE TITLE -->
            <%= render "/shared/bread" %>

            <%= render '/shared/moremenu' %>

            <section class="container">

<!-- INVOICE BODY -->

    <div class="tabs nomargin-top">

                            <!-- tabs -->
                            <ul class="nav nav-tabs">
                                <li class="active">
                                    <a href="#tab1" data-toggle="tab">
                                        <i class="fa fa-money"></i> На продажу
                                    </a>
                                </li>
                                <li>
                                    <a href="#tab2" data-toggle="tab">
                                        <i class="fa fa-cogs"></i> В работе
                                    </a>
                                </li>
                                <li>
                                    <a href="#tab3" data-toggle="tab">
                                        <i class="fa fa-star"></i> Продано
                                    </a>
                                </li>
                            </ul>

                            <!-- tabs content -->
                            <div class="tab-content">
                                <div id="tab1" class="tab-pane active">
                                    



<div class="panel panel-primary">
                    <div class="panel-heading">
                        <h3 class="panel-title">Варианты дизайна <strong>На Продажу</strong></h3>
                    </div>

                    <div class="panel-body">
<% if !@variants0.blank? %> 
                        <p>Список вариантов дизайна, выставленных на продажу для ознакомления на сайте.</p>
                    
                   
                        <table class="table table-striped">
                            <!-- table head -->
                            <thead>
                                <tr>
                                    <th>Артикул</th>
                                    <th>Изображение</th>
                                    <th>Стоимость</th>
                                    <th>Описание</th>
                                    <th>Компания</th>
                                    <th>Ссылка</th>
                                    <th>Дата создания</th>
                                    <th>Статус</th>
                                    <th>Изменить / Удалить</th>

                                </tr>
                            </thead>
                            
                            <!-- table items -->
                            <tbody>
                                <% @variants0.each do |site| %>
                                    <tr><!-- item -->
                                        <td><%= link_to site.articul, site %></td>
                                        <td><%= image_tag site.picture.thumb.url %></td>
                                        <td><%= number_to_currency( site.price, unit: "руб.", :precision => 0, delimiter: " ", format: "%n %u") %></td>
                                        <td><%= truncate( raw( sanitize(site.description, :tags => %w(table tr td), :attributes => %w(id class style) ) ), :length => 100, :omission => "...") %></td>
                                        <td><%= site.company %></td>
                                        <td><%= link_to "Ссылка", site.url, :target => "blank" %></td>
                                        <td><%= Russian::strftime(site.created_at, "%d.%m.%Y") %></td>
                                        <td><%= site.stage %></td>
                                        <td><%= link_to 'Изменить', edit_variant_path(site) %> / <%= link_to 'Удалить', site, method: :delete, data: { confirm: 'Вы уверены?' } %></td>
                                    </tr>
                                <% end %>
                            </tbody>
                        </table>
<% else %>
    <p>На текущий момент не создано ни одного сайта.</p>

<% end %>
                    </div>

                </div>






                                </div>

                                <div id="tab2" class="tab-pane">



<div class="panel panel-primary">
                    <div class="panel-heading">
                        <h3 class="panel-title">Варианты дизайна <strong>На Продажу</strong></h3>
                    </div>

                    <div class="panel-body">
<% if !@variants1.blank? %>  
                        <p>Список вариантов дизайна, выставленных на продажу для ознакомления на сайте.</p>
                    
                  
                        <table class="table table-striped">
                            <!-- table head -->
                            <thead>
                                <tr>
                                    <th>Артикул</th>
                                    <th>Изображение</th>
                                    <th>Стоимость</th>
                                    <th>Описание</th>
                                    <th>Компания</th>
                                    <th>Ссылка</th>
                                    <th>Дата создания</th>
                                    <th>Статус</th>
                                    <th>Изменить / Удалить</th>

                                </tr>
                            </thead>
                            
                            <!-- table items -->
                            <tbody>
                                <% @variants1.each do |site| %>
                                    <tr><!-- item -->
                                        <td><%= link_to site.articul, site %></td>
                                        <td><%= image_tag site.picture.thumb.url %></td>
                                        <td><%= number_to_currency( site.price, unit: "руб.", :precision => 0, delimiter: " ", format: "%n %u") %></td>
                                        <td><%= truncate( raw( sanitize(site.description, :tags => %w(table tr td), :attributes => %w(id class style) ) ), :length => 100, :omission => "...") %></td>
                                        <td><%= site.company %></td>
                                        <td><%= link_to "Ссылка", site.url, :target => "blank" %></td>
                                        <td><%= Russian::strftime(site.created_at, "%d.%m.%Y") %></td>
                                        <td><%= site.stage %></td>
                                        <td><%= link_to 'Изменить', edit_variant_path(site) %> / <%= link_to 'Удалить', site, method: :delete, data: { confirm: 'Вы уверены?' } %></td>
                                    </tr>
                                <% end %>
                            </tbody>
                        </table>
<% else %>
    <p>На текущий момент в работе нет сайтов.</p>

<% end %>
                    </div>

                </div>


                                </div>









  <div id="tab3" class="tab-pane">



<div class="panel panel-primary">
                    <div class="panel-heading">
                        <h3 class="panel-title">Варианты дизайна <strong>На Продажу</strong></h3>
                    </div>

                    <div class="panel-body">
<% if !@variants2.blank? %>   
                        <p>Список вариантов дизайна, выставленных на продажу для ознакомления на сайте.</p>
                    
                 
                        <table class="table table-striped">
                            <!-- table head -->
                            <thead>
                                <tr>
                                    <th>Артикул</th>
                                    <th>Изображение</th>
                                    <th>Стоимость</th>
                                    <th>Описание</th>
                                    <th>Компания</th>
                                    <th>Ссылка</th>
                                    <th>Дата создания</th>
                                    <th>Статус</th>
                                    <th>Изменить / Удалить</th>

                                </tr>
                            </thead>
                            
                            <!-- table items -->
                            <tbody>
                                <% @variants2.each do |site| %>
                                    <tr><!-- item -->
                                        <td><%= link_to site.articul, site %></td>
                                        <td><%= image_tag site.picture.thumb.url %></td>
                                        <td><%= number_to_currency( site.price, unit: "руб.", :precision => 0, delimiter: " ", format: "%n %u") %></td>
                                        <td><%= truncate( raw( sanitize(site.description, :tags => %w(table tr td), :attributes => %w(id class style) ) ), :length => 100, :omission => "...") %></td>
                                        <td><%= site.company %></td>
                                        <td><%= link_to "Ссылка", site.url, :target => "blank" %></td>
                                        <td><%= Russian::strftime(site.created_at, "%d.%m.%Y") %></td>
                                        <td><%= site.stage %></td>
                                        <td><%= link_to 'Изменить', edit_variant_path(site) %> / <%= link_to 'Удалить', site, method: :delete, data: { confirm: 'Вы уверены?' } %></td>
                                    </tr>
                                <% end %>
                            </tbody>
                        </table>
<% else %>
    <p>На текущий момент нет ни одного проданного сайта.</p>

<% end %>
                    </div>

                </div>
                                </div>

                            </div>

                        </div>

                <!-- INVOICE BODY -->

<%= link_to 'Создать новый вариант дизайна', new_variant_path %>
            </section>
        </div>