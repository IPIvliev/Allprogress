            <!-- modal dialog -->
            <div class="modal fade" id="<%= specification %>" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
              <div class="modal-dialog">
                <div class="modal-content">

                  <div class="modal-header"><!-- modal header -->
                    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                    <h4 class="modal-title" id="myModalLabel">ФОРМА ЗАКАЗА</h4>
                  </div><!-- /modal header -->

                  <!-- modal body -->
                  <div class="modal-body">
                               <%= form_for @zakaz, :url => {:controller => "variants" ,:action => "newzakaz"} do |f| %>
  <% if @zakaz.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@zakaz.errors.count, "error") %> prohibited this variant from being saved:</h2>

      <ul>
      <% @zakaz.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
        <div class="form-group">
          <div class="row">
            <div class="col-md-12">
              <label><strong>Как к вам обращаться:</strong></label>
              <%= f.text_field :name, :placeholder => "Иван Дмитриевич", :class => "form-control" %>
            </div>
          </div>
          <div class="row">
            <div class="col-md-12">
              <label><strong>Телефон для связи:</strong></label>
              <%= f.text_field :phone, :placeholder => "8-903-602-8919", :class => "form-control" %>
            </div>
          </div>
        </div>

              <% if @variant %>
                <%= f.hidden_field :theme, :value => "site" %>
                <%= f.hidden_field :variant_id, :value => @variant.id %>
              <% elsif @title == "Сопровождение сайтов" %>
                <%= f.hidden_field :theme, :value => "service" %>
                <%= f.hidden_field :specification, :value => specification %>
              <% elsif @title == "Продвижение сайтов" %>
                <%= f.hidden_field :theme, :value => "advance" %>
              <% end %>

              <div class="row">
                <div class="col-md-12">
                  <%= f.submit :value => "Отправить", :class => "btn btn-lg btn-success" %>
                </div>
              </div>
</div>

<% end %>
                  </div>
                  <!-- /modal body -->

                </div>
              </div>
